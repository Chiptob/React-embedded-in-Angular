# Implementation of React Component in Angular 

This small project shows an example of how a React component can be implemented in an Angular application.

## Run

Run `ng serve --open`

## Build

Run `ng build` to build the project.
